import { Typography } from "@mui/material";
import {
  useLazyGetPostQuery,
  useLazyGetPostsQuery,
  useLazyGetPostCommetsQuery,
} from "../store/rtk";

const Posts = () => {
  return <Typography>Posts Screen</Typography>;
};

export default Posts;
